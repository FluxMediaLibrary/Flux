<div align="center">
  <img src="packages/frontend/public/icon-512.png" width="96" alt="Flux" />
  <h1>Flux</h1>
  <p>Self-hosted media library platform — torrent acquisition, TMDb-backed requests, Netflix-style profiles, HLS streaming.</p>

  [![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?logo=typescript&logoColor=fff)](#)
  [![Next.js](https://img.shields.io/badge/Next.js-000?logo=nextdotjs&logoColor=fff)](#)
  [![React](https://img.shields.io/badge/React-087EA4?logo=react&logoColor=fff)](#)
  [![Fastify](https://img.shields.io/badge/Fastify-000?logo=fastify&logoColor=fff)](#)
  [![Prisma](https://img.shields.io/badge/Prisma-2D3748?logo=prisma&logoColor=fff)](#)
  [![PostgreSQL](https://img.shields.io/badge/PostgreSQL-4169E1?logo=postgresql&logoColor=fff)](#)
  [![Redis](https://img.shields.io/badge/Redis-DC382D?logo=redis&logoColor=fff)](#)
  [![Docker](https://img.shields.io/badge/Docker-2496ED?logo=docker&logoColor=fff)](#)
  [![FFmpeg](https://img.shields.io/badge/FFmpeg-007808?logo=ffmpeg&logoColor=fff)](#)
</div>

---

## Tech stack

<table>
  <tr>
    <td width="50%">

### Backend
| Component | Tech |
|---|---|
| Runtime | Node.js 22 |
| Framework | Fastify 5 |
| Language | TypeScript (strict) |
| ORM | Prisma |
| Database | PostgreSQL 16 |
| Job queue | BullMQ (Redis 7) |
| Auth | JWT + argon2 |
| Validation | Zod |
| Torrents | Transmission (sidecar) |
| Transcoding | FFmpeg (on-demand HLS) |
| Mobile client | Android WebView shell |

  </td>
    <td width="50%">

### Frontend
| Component | Tech |
|---|---|
| Framework | Next.js 16 (App Router) |
| UI | React 19 |
| Language | TypeScript (strict) |
| Streaming | hls.js |
| Icons | Inline SVG |
| Styling | CSS (globals.css) |
| Auth | JWT (HttpOnly + localStorage) |

  </td>
  </tr>
</table>

<table>
  <tr>
    <td width="50%">

### Infrastructure
| Component | Tech |
|---|---|
| Container | Docker Compose |
| Reverse proxy | nginx |
| OS target | Ubuntu VPS |
| Database | `postgres:16-alpine` |
| Cache | `redis:7-alpine` |
| Torrent client | `linuxserver/transmission` |

  </td>
    <td width="50%">

### External APIs
| Service | Purpose |
|---|---|
| TMDb v3 | Metadata, search, discover, cast |
| Discord webhook | Notification delivery |
| SMTP | Email notifications |

  </td>
  </tr>
</table>

## Architecture

### Account vs Profile

- **User (account)**: the login. Holds email, password, and role (`ADMIN` / `MEMBER`). Owns one or more profiles.
- **Profile**: Netflix-style sub-profile under an account. Watch progress, requests, and continue-watching all key off `profileId`.

JWT carries `sub` (accountId) + `role`. After selecting a profile, the token also carries `activeProfileId` for per-profile routes.

### Access control

| Route group | Guard |
|---|---|
| `/admin/*` | `role === ADMIN` on the JWT |
| `/profiles` | Logged in (any role) |
| `/(member)/*` | Logged in + active profile selected |
| `/(auth)/*` | Public (unauthenticated) |

Signup is invite-only: requires a valid, unexpired, single-use invite code generated by an admin.

### How media is joined

TMDb ID (`tmdbId` + `mediaType`) joins the library to requests. Library items are matched to TMDb at ingest. Requests use the same key so a fulfilled request automatically links to the matching library item.

### Playback

1. **Direct play** — serves the original file via byte-range endpoint. Works for browser-native codecs (MP4/WebM/H.264+AAC).
2. **HLS fallback** — on-demand FFmpeg transcode streamed through hls.js when the browser cannot decode the source.

Playback decisions are made per-file by probing codec support at the server level.

## Prerequisites

- Docker and Docker Compose
- [TMDb API key](https://www.themoviedb.org/settings/api) (v3, free)
- A domain pointed at your VPS (for frontend origin + invite links)
- At least 1 GB RAM free for transcoding

## Setup

Flux can run two ways:

1. **Published images**: best for normal self-hosting. Docker pulls the backend/frontend images from GHCR and starts them with Postgres, Redis, and Transmission.
2. **Source build**: best for local development or when you are changing the code. Docker builds the backend/frontend images from this checkout.

### 1. Clone

```bash
git clone https://github.com/IDKDeadXD/Flux.git flux
cd flux
```

### 2. Configure environment

```bash
cp .env.example .env
```

Edit `.env` and fill in the required values:

| Variable | Purpose |
|---|---|
| `TMDB_API_KEY` | TMDb v3 API key |
| `JWT_SECRET` | Long random string (`openssl rand -hex 48`) |
| `POSTGRES_PASSWORD` | Strong database password |
| `BOOTSTRAP_ADMIN_EMAIL` | Initial admin email |
| `BOOTSTRAP_ADMIN_PASSWORD` | Initial admin password |
| `FRONTEND_ORIGIN` | Public frontend URL (e.g. `https://flux.example.com`) |
| `PUBLIC_API_BASE_URL` | Public backend URL for stream/cast URLs. Use the same public origin when nginx routes `/api` to the backend, or a backend-specific URL for split-domain installs. |

Recommended values for a single-domain VPS:

```env
FRONTEND_ORIGIN=https://flux.example.com
PUBLIC_API_BASE_URL=https://flux.example.com
NEXT_PUBLIC_API_BASE_URL=
FLUX_INTERNAL_API_BASE_URL=http://127.0.0.1:6948
```

`NEXT_PUBLIC_API_BASE_URL` is only needed when the browser should call a separate backend domain directly, such as `https://api.flux.example.com`. For the published frontend image, leave it empty and let the frontend proxy `/api/...` to `FLUX_INTERNAL_API_BASE_URL` at runtime.

### 3. Start With Published Images

This is the normal path for users who just want to pull images and run Flux:

```bash
docker compose -f docker-compose.images.yml pull
docker compose -f docker-compose.images.yml up -d
```

The image Compose file uses:

| Service | Image |
|---|---|
| Backend | `ghcr.io/idkdeadxd/flux-backend:latest` |
| Frontend | `ghcr.io/idkdeadxd/flux-frontend:latest` |
| PostgreSQL | `postgres:16-alpine` |
| Redis | `redis:7-alpine` |
| Transmission | `lscr.io/linuxserver/transmission:latest` |

To pin a specific Flux image tag:

```bash
FLUX_IMAGE_TAG=prod docker compose -f docker-compose.images.yml up -d
```

To use images from a fork or private registry:

```bash
FLUX_IMAGE_PREFIX=ghcr.io/your-name/flux docker compose -f docker-compose.images.yml up -d
```

On first run the backend applies pending Prisma migrations, then seeds the bootstrap admin account if no users exist yet.

### 4. Start From Source

Use this when developing or when you want Docker to build local changes:

```bash
docker compose up --build
```

### 5. Image Publishing

The `Publish Images` GitHub Actions workflow builds and pushes both app images to GitHub Container Registry.

Image tags:

| Tag | When it is published |
|---|---|
| `latest` | Pushes to `main`, `master`, or `PROD` |
| `prod` | Pushes to `PROD` |
| `sha-<commit>` | Every image workflow run |
| `v*` | Version tags, such as `v1.2.0` |

To publish images from your own fork:

1. Enable GitHub Actions for the repository.
2. Make sure the workflow has package write permission. The repo setting is usually **Settings -> Actions -> General -> Workflow permissions -> Read and write permissions**.
3. Push to `main`, `master`, `PROD`, or push a `v*` tag.
4. Use your fork's image prefix when deploying:

```bash
FLUX_IMAGE_PREFIX=ghcr.io/your-name/flux docker compose -f docker-compose.images.yml up -d
```

### 6. Reverse Proxy (nginx)

```
# Frontend — Next.js standalone
location / {
    proxy_pass http://127.0.0.1:4938;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}

# Backend — Fastify API
location /api/ {
    proxy_pass http://127.0.0.1:6948;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

### 7. First Login

Navigate to your frontend URL, sign in with the bootstrap admin credentials, then:

1. Create profiles via `/profiles`
2. Generate invite codes via `/admin/invites`

## Services

| Service | Image | Port | Public |
|---|---|---|---|
| Frontend | custom (`Dockerfile`) | 4938 | yes |
| Backend | custom (`Dockerfile`) | 6948 | yes |
| PostgreSQL | `postgres:16-alpine` | 5432 | loopback only |
| Redis | `redis:7-alpine` | 6379 | loopback only |
| Transmission RPC | `linuxserver/transmission` | 9091 | loopback only |
| Transmission peer | `linuxserver/transmission` | 51413 | TCP + UDP |

Transmission publishes `51413/tcp` and `51413/udp` for BitTorrent peers. On a VPS, the host firewall or cloud firewall must also allow both protocols, otherwise downloads can still start but may sit at 0 peers on weak tracker swarms. The admin torrent page reports whether Transmission's peer port test is open.

## Routes

### `/` (root)
Redirects based on auth state: unauthenticated → `/login`, logged in → `/home`, no profile → `/profiles`.

### Auth (public)

| Route | Description |
|---|---|
| `/login` | Sign in with email + password |
| `/signup` | Create account (requires invite code) |

### Member (requires profile)

| Route | Description |
|---|---|
| `/home` | Continue watching, recently added, genre rows |
| `/browse` | TMDb discover (trending, popular, genre filter) |
| `/library` | Library grid with type/genre/year filters |
| `/library/[id]` | Detail page (cast, episodes, play) |
| `/watch/[id]` | Video player (Plex-style UI) |
| `/profiles` | Profile selection and management |

### Admin (requires ADMIN role)

| Route | Description |
|---|---|
| `/admin/info` | System dashboard (uptime, storage, DB counts) |
| `/admin/torrents` | Upload .torrent files, manage downloads |
| `/admin/requests` | View and approve/reject media requests |
| `/admin/invites` | Generate single-use invite codes |
| `/admin/settings` | Notification config (Discord/SMTP) |

## API

REST/JSON under `/api`. JWT in `Authorization: Bearer <token>`. All input validated with Zod, all errors returned as `{ error, message, statusCode }`.

### Modules

| Module | Endpoints |
|---|---|
| Auth | `POST /api/auth/login`, `POST /api/auth/signup` |
| Profiles | `GET/POST /api/profiles`, `PATCH/DELETE /api/profiles/:id`, `POST /api/profiles/:id/activate` |
| Invites | `POST/GET/DELETE /api/invites` (admin) |
| Library | `GET /api/library`, `GET /api/library/:id`, `GET /api/library/:id/play` |
| Streaming | `GET /api/stream/:id`, `GET /api/stream/:id/hls/:file` |
| TMDb | `GET /api/tmdb/search`, `GET /api/tmdb/:type/:id`, `GET /api/tmdb/discover/*` |
| Requests | `POST/GET /api/requests` (per-profile), `PATCH /api/requests/:id` (admin) |
| Torrents | `POST/GET/DELETE /api/torrents` (admin) |
| Notifications | `GET/PUT /api/admin/notifications/settings` (admin) |
| Admin info | `GET /api/admin/info` (admin) |

## Data model

```
User ──< Profile ──< WatchProgress
  │                    │
  │                    ├──< Request ──< Torrent
  │
  ├──< Invite (created)
  └──< Invite (used)

MediaItem ──< Episode ──< WatchProgress
  │
  ├──< Torrent
  └──< WatchProgress

NotificationSettings (singleton)
```

| Model | Purpose |
|---|---|
| `User` | Login account (email, password, role) |
| `Profile` | Sub-profile under a user |
| `Invite` | Single-use, expiring signup code |
| `MediaItem` | Library entry matched to TMDb (movie or show) |
| `Episode` | Per-season, per-episode file for shows |
| `Torrent` | Download with status tracking and live stats |
| `Request` | Per-profile media request keyed to TMDb ID |
| `WatchProgress` | Per-profile save state (position, duration, completed) |
| `NotificationSettings` | Singleton for Discord/SMTP config |

## Development

```bash
# One-time setup
cp .env.example .env       # point DB/Redis at localhost
npm install
npm run prisma:generate
npm run prisma:migrate:dev

# Run locally
npm run dev:backend        # backend on :6948 (hot reload)
npm run dev:frontend       # frontend on :3000 (hot reload)
```

## Community

- Read [CONTRIBUTING.md](CONTRIBUTING.md) before opening a pull request.
- Report reproducible bugs and feature requests with the GitHub issue templates.
- Report security vulnerabilities privately through [SECURITY.md](SECURITY.md).
- Use [SUPPORT.md](SUPPORT.md) for setup and troubleshooting guidance.

### Monorepo layout

```
flux/
├── docker-compose.yml
├── .env.example
├── shared/                        # @flux/shared — API contract (DTOs + enums)
│   └── src/index.ts
├── packages/
│   ├── backend/
│   │   ├── prisma/schema.prisma   # central data contract
│   │   └── src/
│   │       ├── modules/           # domain modules (auth, profiles, library, etc.)
│   │       ├── jobs/              # BullMQ job processors
│   │       └── lib/               # utilities (jwt, db, errors, webtorrent)
│   └── frontend/
│       ├── app/                   # Next.js App Router
│       │   ├── (auth)/            # login, signup
│       │   ├── (member)/          # home, browse, library, watch
│       │   ├── admin/             # info, torrents, requests, invites, settings
│       │   └── profiles/          # profile selection
│       └── components/            # shared UI (player, navbar, guards, etc.)
└── transmission-settings.json     # Transmission config (private tracker defaults)
```

### Scripts

| Command | Description |
|---|---|
| `npm run dev` | Full Docker Compose stack |
| `npm run dev:backend` | Backend with hot reload |
| `npm run dev:frontend` | Frontend with hot reload |
| `npm run build` | Build all workspaces |
| `npm run typecheck` | Type-check all workspaces |
| `npm run lint` | Lint all workspaces |
| `npm run prisma:migrate` | Apply pending migrations |
| `npm run prisma:migrate:dev` | Create and apply a new migration |
| `npm run prisma:generate` | Regenerate Prisma client |
### Conventions

- TypeScript strict, ES modules, no `any` without explicit reason
- DTOs in `@flux/shared` — never redefine wire shapes on either side
- DB changes: edit `schema.prisma` then run `npm run prisma:migrate` (named migration)
- Path traversal protection: all media paths resolved against configured root
- Secrets: env only, never committed. TMDb key stays server-side

## License

Flux is released under the [MIT License](LICENSE).
